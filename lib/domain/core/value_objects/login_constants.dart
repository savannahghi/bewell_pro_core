const int kTokenExpiryDuration = 3300;

/// if `kTimeOutAfter` is the app idle time that triggers the automatic logout
/// used in `home_page.dart`
const int kTimeOutAfter = 1800;

const int kModalCountDown = 15;

// the amount of seconds before token expiry where we should refresh
// the token
const int kTokenPreExpiryThreshold = 600;

const String logoutMessage = 'Hold on ... you\'ll be logged out in a moment';
